# 구현 완료 요약 (쇼핑몰 메인 로고 업로드 및 헤더 연동)

요청하신 **어드민 테마 설정 페이지(`/admin/theme`)에서 쇼핑몰 상단 헤더에 들어갈 로고 이미지를 직접 업로드(Upload)하여 저장하고 적용하는 기능**을 구현 완료했습니다. 로고 이미지를 따로 지정하지 않을 시 기존 텍스트인 "PERVADE"가 그대로 노출되는 롤백 대응까지 완비했습니다.

## 🛠️ 구현 및 변경 완료 사항

### 1. 로고 이미지 직접 업로드 API 신설 (`/api/admin/theme/upload-logo`)
*   **서버 로컬 물리 저장 연동**:
    *   어드민 화면에서 전달된 로고 이미지 파일 바이너리를 받아 `public/uploads` 디렉토리에 고유 타임스탬프 파일명(예: `logo_1786942825.png`)으로 저장합니다.
    *   저장 완료 시 해당 로고의 가상 상대 경로(`/uploads/logo_1786942825.png`)를 클라이언트에 응답으로 반환합니다.
*   **테마 API 수정**:
    *   `/api/admin/theme` GET/POST API가 `LOGO_URL` 변수를 신규 연동 및 DB에 영구 적재/수정하도록 DB 매핑 영역을 확장했습니다.

### 2. 어드민 내 로고 업로드 및 설정 UI 구축 (`src/app/admin/theme/page.tsx`)
*   **로고 전용 설정 블록 신설**:
    *   어드민 페이지의 1단(왼쪽 컬럼) 내에 **`2. 쇼핑몰 메인 로고 설정`** 섹션을 새롭게 배치했습니다.
    *   배경 동영상과 유사하게 **`외부 이미지 URL`** 입력 방식과 **`로고 이미지 직접 업로드`** 방식을 버튼으로 선택할 수 있도록 설계했습니다.
*   **실시간 미리보기 및 초기화 지원**:
    *   로고가 선택되면 백엔드 API를 거쳐 즉시 업로드되고, 어드민 내 블랙박스 스타일 컨테이너에서 로고의 가시성과 형태를 실시간 미리보기로 보여줍니다.
    *   **[로고 초기화]**를 누르면 저장된 로고 이미지가 지워지고 텍스트 로고 "PERVADE" 기본 상태로 돌아갑니다.

### 3. 쇼핑몰 헤더 네비게이션 동적 바인딩 (`src/components/layout/Navbar.tsx`)
*   **반응형 로고 렌더링**:
    *   쇼핑몰 상단 헤더 컴포넌트(`Navbar`)가 마운트되는 시점에 테마 설정값(`LOGO_URL`)을 조회합니다.
    *   DB에 저장된 로고 이미지 주소가 유효할 경우, 기존 텍스트 "PERVADE" 대신 **그 자리에 깔끔하게 로고 이미지 파일이 스케일링되어 렌더링**됩니다.
    *   로고 URL이 존재하지 않거나 빈 값일 경우, 텍스트 형태의 브랜드 로고 "PERVADE"가 그대로 노출되어 서비스 중단 없는 안전성(Fallback)을 확보했습니다.

## 💡 직접 테스트해보기!
1.  **[테마 관리자 접속]**: **[테마 설정 페이지](http://localhost:3000/admin/theme)**에 들어갑니다.
2.  **[로고 이미지 업로드]**:
    *   새로 추가된 **`2. 쇼핑몰 메인 로고 설정`** 섹션에서 **`로고 이미지 직접 업로드`**를 클릭합니다.
    *   원하는 쇼핑몰 로고 이미지 파일을 고릅니다 (투명 배경 png 또는 svg 파일 추천).
    *   업로드가 완료되면 검정색 미리보기 박스 안에 내 로고가 세련되게 뜨는 것을 확인합니다.
3.  **[변경사항 저장]**:
    *   우측 상단의 **`변경사항 저장`**을 누르고 메인 쇼핑몰 페이지로 이동합니다.
4.  **[헤더 확인]**:
    *   **[쇼핑몰 메인 페이지](http://localhost:3000/)**에 접속하면, 헤더 네비게이션 바 좌측에 텍스트 대신 **내가 방금 업로드하여 설정한 로고 이미지가 이쁘게 노출되는 모습을 실시간으로 확인**할 수 있습니다!
